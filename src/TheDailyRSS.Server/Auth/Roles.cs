namespace TheDailyRSS.Server.Auth;

/// <summary>Application role names. Used for both the Identity role and the authorization policy.</summary>
public static class Roles
{
    public const string Admin = "Admin";
}
